import { createTheme, Theme } from '@mui/material';
export const muiTheme: Theme = createTheme({
  palette: {
    primary: {
      main: '#008A00',
      light: '#befabe',
    },
    secondary: {
      main: '#2a3eb1',
      light: '#3d5afe',
    },
  }
});
