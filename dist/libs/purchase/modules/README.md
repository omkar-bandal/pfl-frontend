# purchase-modules

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build purchase-modules` to build the library.

## Running unit tests

Run `nx test purchase-modules` to execute the unit tests via [Jest](https://jestjs.io).
